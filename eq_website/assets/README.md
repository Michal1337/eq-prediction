# Assets

This directory contains additional assets for our website. Tough those files aren't directly needed for the application to work, they might be useful to facilitate app setup.

## File structure

* `data` - data sources, csv files used to populate the db
* `img` - images, primarily used in README's
* `models` - prediction model files, used to populate the db
* `notebooks` - additional preprocessing